# celestia-node-exporter
otel collector with prometheus exporter

## Usage

### celestia mainnet
```
cp .env.celestia .env
docker compose up -d
```

### mocha testnet
```
cp .env.mocha .env
docker compose up -d
```